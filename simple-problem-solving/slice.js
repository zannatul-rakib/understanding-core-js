const friendsAge = [32, 30, 39, 45, 23, 21, 14, 16, 19, 17];
const partial = friendsAge.slice(3, 7);
console.log(partial);
console.log(friendsAge);

// note: when slice an array for remove some numbers then the original array couldn't be changed
